<%= simple_form_for @build do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <% if @build.id.nil? %>
    <%= f.submit 'Create' %>
  <% else %>
    <%= f.submit 'Update' %>
  <% end %>
<% end %>
